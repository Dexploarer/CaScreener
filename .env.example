# At least one for real AI streaming; otherwise demo streams are used
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
OPENROUTER_API_KEY=sk-or-v1-...
# Optional override (default in code is openrouter/aurora-alpha)
# OPENROUTER_MODEL=openrouter/aurora-alpha

# Helius (Solana) – for Wallet Analytics dashboard
HELIUS_API_KEY=your-helius-api-key

# Allium – optional enrichment (multi-chain, analytics). Get key: https://app.allium.so/join
# ALLIUM_API_KEY=your-allium-api-key
# ALLIUM_API_BASE=https://api.allium.so

# CoinGecko Demo API key (optional; higher rate limits than free tier)
# COINGECKO_API_KEY=CG-...

# PumpPortal (Solana memecoin data — pump.fun, PumpSwap)
# PUMPPORTAL_API_KEY=your-pumpportal-api-key
# PUMPPORTAL_WS_URL=wss://pumpportal.fun/api/data
# PUMPPORTAL_SNAPSHOT_WINDOW_MS=4000
# PUMPPORTAL_TOKEN_TRADES_WINDOW_MS=3000
# PUMPPORTAL_CONNECTION_TIMEOUT_MS=8000

# Prediction markets (optional overrides; both have public read APIs)
# POLYMARKET_API_BASE=https://gamma-api.polymarket.com
# MANIFOLD_API_BASE=https://api.manifold.markets

# Token discovery (optional override)
# DEXSCREENER_API_BASE=https://api.dexscreener.com

# Convex meme-meta memory + RAG (optional)
# CONVEX_URL=https://<your-deployment>.convex.cloud
# CONVEX_DEPLOY_KEY=your-convex-deploy-key
# CONVEX_MEME_NAMESPACE=solana-memecoins
# CONVEX_REQUEST_TIMEOUT_MS=2500
# In Convex dashboard environment variables, set OPENROUTER_API_KEY for RAG embeddings.
# Optional override:
# OPENROUTER_EMBEDDING_MODEL=openai/text-embedding-3-small

# Convex R2 (for durable share video mirrors) - set these in Convex env
# R2_TOKEN=...
# R2_ACCESS_KEY_ID=...
# R2_SECRET_ACCESS_KEY=...
# R2_ENDPOINT=...
# R2_BUCKET=...
